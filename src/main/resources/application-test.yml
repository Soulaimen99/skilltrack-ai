quarkus:
  datasource:
    db-kind: postgresql
    devservices:
      enabled: true
      image-name: postgres:15
    sql:
      load-script: import.sql

  hibernate-orm:
    database:
      generation: drop-and-create
